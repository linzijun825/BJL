<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>線上百家樂</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #2e8b57;
            color: white;
        }
        .game-container {
            margin: 50px auto;
            padding: 20px;
            background-color: #222;
            border-radius: 10px;
            width: 400px;
        }
        .buttons {
            margin: 20px 0;
        }
        button {
            padding: 10px 20px;
            margin: 5px;
            font-size: 18px;
        }
        .result, .balance, .history {
            font-size: 24px;
            margin-top: 20px;
        }
        input {
            padding: 10px;
            margin: 10px;
            font-size: 18px;
        }
        .cards {
            margin: 20px auto;
            display: flex;
            justify-content: center;
        }
        .card {
            width: 60px;
            height: 90px;
            margin: 0 5px;
            background-color: white;
            border-radius: 10px;
            position: relative;
            transform-style: preserve-3d;
            transition: transform 0.8s;
            cursor: pointer;
        }
        .card.flipped {
            transform: rotateY(180deg);
        }
        .card .front, .card .back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 10px;
        }
        .card .front {
            background-color: red;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 24px;
        }
        .card .back {
            background-color: blue;
            transform: rotateY(180deg);
        }
    </style>
</head>
<body>
    <div class="game-container">
        <h1>線上百家樂</h1>
        <div class="balance" id="balance">玩家餘額: $1000</div>
        <input type="number" id="betAmount" placeholder="輸入下注金額" min="1" />
        <div class="cards" id="cards"></div>
        <div class="buttons">
            <button onclick="placeBet('player')">下注閒家</button>
            <button onclick="placeBet('banker')">下注莊家</button>
            <button onclick="placeBet('tie')">下注和局</button>
        </div>
        <div class="result" id="result">請選擇下注</div>
        <button onclick="resetGame()">重置遊戲</button>
        <div class="history" id="history">遊戲歷史：</div>
    </div>

    <script src="game.js"></script>
</body>
</html>
